# Scheduler Supabase Touch

This repository runs a scheduled GitHub Action that makes a harmless read request to the ServeWell Scheduler Supabase project so the free project stays active.

The workflow does not modify scheduler data.
